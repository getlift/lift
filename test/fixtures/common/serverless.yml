service: storage
configValidationMode: error

provider:
    name: aws

constructs:
    testStorage:
        type: storage

resources:
    Resources:
        UserDefinedResource:
            Type: AWS::S3::Bucket
