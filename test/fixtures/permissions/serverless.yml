service: storage
configValidationMode: error
disabledDeprecations:
    - PROVIDER_IAM_SETTINGS_V3

provider:
    name: aws

functions:
    foo:
        handler: worker.handler

constructs:
    testStorage:
        type: storage
