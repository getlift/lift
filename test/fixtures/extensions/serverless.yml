service: storage
configValidationMode: error

provider:
    name: aws

constructs:
    nonExistingKeyExtension:
        type: storage
        extensions:
            notExisting:
                Properties:
                    ObjectLockEnabled: True
