service: storage

provider:
  name: aws

constructs:
  stripe:
    type: webhook
    authorizer:
      handler: authorizer.main
    path: /stripe-webhook
  github:
    type: webhook
    insecure: true
    path: /github-webhook
