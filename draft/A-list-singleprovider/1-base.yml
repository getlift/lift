service: shop

provider:
    name: aws
    region: us-east-1
    # ...

apis:
    backend:
        routes:
            'GET /':
                handler: src/index.handler
            'POST /booking':
                handler: src/create-booking.handler

queues:
    orders:
        worker:
            handler: src/worker.handler
