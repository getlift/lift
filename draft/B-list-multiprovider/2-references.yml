service: shop

provider:
    name: aws
    region: us-east-1
    # ...

apis:
    backend:
        routes:
            'POST /booking':
                handler: src/create-booking.handler
                environment:
                    QUEUE_URL: ${queues:orders.queueUrl}

queues:
    orders:
        worker:
            handler: src/worker.handler
