service: lift-app

provider:
    name: aws
    region: us-east-1
    runtime: nodejs12.x

custom:
    lift:
        use: lift
        s3:
            photos:

functions:
    hello:
        handler: demo/handler.hello

package:
    exclude:
        - 'bin/**'
        - 'dist/**'
        - 'node_modules/**'
        - 'src/**'
        - 'test/**'
        - 'utils/**'
        - 'website/**'

plugins:
    - ./dist/plugin.js
